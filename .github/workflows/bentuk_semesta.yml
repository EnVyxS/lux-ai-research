name: bentuk-semesta

on:
  push:
    paths:
      - "lux_ai/serapan/bentuk_semesta.py"
      - ".github/workflows/bentuk_semesta.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  bentuk:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Jalankan probe bentuk
        run: |
          set +e
          python -m lux_ai.serapan.bentuk_semesta > bentuk.log 2>&1
          echo "KODE=$?" >> $GITHUB_ENV
          mkdir -p reports
          tail -c 8000 bentuk.log > reports/bentuk_semesta.log
          exit 0
      - name: Tulis status
        run: |
          printf '{\n  "run_id": "%s",\n  "commit": "%s",\n  "waktu_utc": "%s",\n  "kode_keluar": %s\n}\n' \
            "$GITHUB_RUN_ID" "$GITHUB_SHA" "$(date -u +%Y-%m-%dT%H:%M:%SZ)" "${KODE:-99}" \
            > reports/bentuk_semesta_status.json
      - name: Commit laporan
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add reports/bentuk_semesta.json reports/bentuk_semesta.log reports/bentuk_semesta_status.json || true
          git commit -m "Laporan bentuk semesta [skip ci]" || echo "tidak ada perubahan"
          git pull --rebase --autostash
          git push
          exit 0
