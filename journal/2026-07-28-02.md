# 2026-07-28-02 — Tugas 2 dimulai: pengukuran sebelum serapan

Ditulis SEBELUM hasil run probe terlihat.

## Yang dikerjakan

1. Membaca `lux-research` secara baca-saja atas izin operator: struktur akar,
   `lux/`, `lux/validasi/` (`dsr.py` 8.328 byte blob
   `d87924b6e6b3f54dbbe4959260dc22c501b9c997`; `pbo.py` 10.097 byte blob
   `630898cf933cb626fa4ee4a634adb56d91956a2b`), dan `lux/binance_vision.py`
   (blob `f45b91420f4911de582fb51edc7e71cacd7d63ab`). TIDAK ADA hasil, angka,
   atau putusan dari repo itu yang dibawa ke sini. Pengangkatan DSR/PBO belum
   dilakukan; bila dilakukan, akan disertai catatan pengangkatan.
2. Menguji akses jaringan sandbox: GAGAL (`Could not resolve host`). Konsekuensi
   dicatat di ADR-A002: seluruh pengukuran lewat runner.
3. Menulis sendiri klien arsip (`lux_ai/serapan/arsip.py`), pembaca klines
   (`klines.py`), pengukur probe (`probe.py`), dan workflow `probe-serapan`.
4. ADR-A002 diterima sebagian; bagian estimasi ukuran sengaja kosong.

## Ramalan (ditulis sebelum run)

- **R-1:** indeks arsip `monthly/klines/` memuat lebih dari 450 simbol, jauh di
  atas jumlah pair aktif hari ini. Bila angkanya justru mendekati jumlah pair
  aktif, listing saya salah baca dan pipeline harus berhenti.
- **R-2:** setidaknya tiga dari empat klaim delisting terbukti ada di indeks.
- **R-3:** satu simbol-bulan 1m simbol likuid berkisar 1,5-4 MB zip, dan parquet
  zstd-nya lebih kecil daripada zip-nya.
- **R-4:** estimasi total dari ekstrapolasi probe akan LEBIH BESAR dari kisaran
  warisan 40-60 GB, karena probe condong likuid.
- **R-5:** berkas bulanan terbaru memuat baris header, yang lama tidak. Bila
  seragam, deteksi header saya berlebihan tetapi tidak berbahaya.

## Yang belum diketahui

Ukuran sebenarnya, jumlah simbol, cacah berkas, dan apakah checksum arsip cocok.
Semua menunggu artefak run.
