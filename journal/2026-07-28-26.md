# Jurnal 2026-07-28 sesi 26

## Keadaan masuk

STATE v16 (`dd996064…`) dan PROMPT v16 (`93a28502…`) sudah sinkron dan sudah
dibaca ulang dari `main`. Utang aktif: 7, 24, 25, 26. Ramalan berikutnya R-56.

## Pekerjaan sesi ini

Utang 7: memeriksa `lux_ai/serapan/arsip.py` terhadap KC-9 (aturan 32) pada tiga
titik — URL, nama berkas keluaran, kunci manifes.

## Pra-registrasi (DITULIS SEBELUM BERKAS DIBACA)

Saya belum membuka `arsip.py` sejak KC-9 ditemukan. Ingatan saya atas modul itu
berasal dari peta API, bukan dari pembacaan baris. Karena itu ketiga ramalan di
bawah ditulis dulu, dan pembacaan dilakukan sesudahnya.

- **R-56.** `url_klines` dan `url_funding` menyusun URL dengan menempelkan nama
  simbol mentah (mis. lewat f-string atau `+`), TANPA `urllib.parse.quote`.
  Besaran pendamping (aturan 26): cacah titik penempelan nama simbol ke dalam
  string URL di seluruh berkas adalah 2 sampai 5.
- **R-57.** Nama berkas keluaran (zip singgahan dan/atau parquet) dibentuk dari
  nama simbol apa adanya, tanpa pengamanan nama berkas. Besaran pendamping:
  cacah titik pembentukan nama berkas dari nama simbol adalah 1 sampai 4.
- **R-58.** Tidak ada satu pun tempat di `arsip.py` yang sudah menormalkan atau
  memeriksa nama simbol non-ASCII. Besaran pendamping: cacah pemanggilan
  `quote`, `encode`, `normalize`, atau penyaring aksara atas nama simbol adalah
  tepat 0.

Catatan aturan 30: penyebut ketiga ramalan ini adalah jumlah baris `arsip.py`
yang benar-benar saya baca. Bila berkas gagal dibaca, ketiganya TIDAK
TERADJUDIKASI, bukan TEPAT.

Catatan aturan 20: ramalan ini hanya tentang `arsip.py`. Modul `klines.py` dan
jalur serapan lain belum disampel dan tidak boleh ikut disimpulkan.

## Adjudikasi

Belum. Diisi setelah pembacaan.
