# Jurnal 2026-07-28 sesi 26

## Keadaan masuk

STATE v16 (`dd996064…`) dan PROMPT v16 (`93a28502…`) sudah sinkron dan sudah
dibaca ulang dari `main`. Utang aktif: 7, 24, 25, 26. Ramalan berikutnya R-56.

## Pekerjaan sesi ini

Utang 7: memeriksa `lux_ai/serapan/arsip.py` terhadap KC-9 (aturan 32) pada tiga
titik — URL, nama berkas keluaran, kunci manifes.

## Pra-registrasi (DITULIS SEBELUM BERKAS DIBACA)

Didorong lebih dulu pada commit `de727c09929ec9c5faab497036644c489f080455`,
sebelum `arsip.py` dibuka. Saya belum membaca modul itu sejak KC-9 ditemukan;
ingatan saya berasal dari peta API, bukan dari baris kode.

- **R-56.** `url_klines` dan `url_funding` menyusun URL dengan menempelkan nama
  simbol mentah, TANPA `urllib.parse.quote`. Besaran pendamping: cacah titik
  penempelan nama simbol ke dalam string URL adalah 2 sampai 5.
- **R-57.** Nama berkas keluaran dibentuk dari nama simbol apa adanya, tanpa
  pengamanan nama berkas. Besaran pendamping: cacah titik pembentukan nama
  berkas dari nama simbol adalah 1 sampai 4.
- **R-58.** Tidak ada satu pun tempat di `arsip.py` yang sudah menormalkan atau
  memeriksa nama simbol non-ASCII. Besaran pendamping: cacah pemanggilan
  `quote`, `encode`, `normalize`, atau penyaring aksara atas nama simbol adalah
  tepat 0.

## Yang dibaca

`lux_ai/serapan/arsip.py`, blob `0104958bd99772cda8262d5527da52aea9635724`,
dibaca UTUH dari `main` pada commit `de727c09`. Penyebut (aturan 30): seluruh
berkas, 14 definisi tingkat atas. Bukan sampel.

## Adjudikasi — ketiganya MELESET, dan saya senang meleset

- **R-56 MELESET.** `arsip.py` punya pembantu khusus:
  `segmen(teks) -> urllib.parse.quote(teks, safe="")`, dan BAIK `url_klines`
  MAUPUN `url_funding` memanggilnya lebih dulu (`s = segmen(simbol)`) sebelum
  menyisipkan nama ke f-string. Besaran pendamping jatuh di dalam pita 2..5
  (lima titik: dua di `url_klines`, dua di `url_funding`, satu prefix di
  `bulan_tersedia`), tetapi klausa utamanya salah, jadi baris ini MELESET, bukan
  MELESET SEPARUH: yang saya ramalkan cacat ternyata sudah benar.
- **R-57 MELESET.** `arsip.py` tidak membentuk nama berkas dari nama simbol sama
  sekali. Satu-satunya penulis berkas adalah `simpan(data, tujuan)`, dan
  `tujuan` datang dari pemanggil. Cacah titik = **0**, di luar pita 1..4.
- **R-58 MELESET.** Ada `urllib.parse.quote` di `segmen` dan `urllib.parse.
  urlencode` di `_halaman`. Cacah ≥ 2, bukan 0.

Papan skor menjadi R-1..R-58: TEPAT 31, MELESET **18**, MELESET SEPARUH 2,
TIDAK TERADJUDIKASI 1, MENUNGGU 6. 31+18+2+1+6 = 58 (aturan 21).

## Apa yang sebenarnya saya temukan

Docstring `arsip.py` butir 4 sudah berbunyi: "Nama simbol wajib di-percent-encode
saat menyusun URL CDN; arsip pernah memuat simbol non-ASCII, dan listing bisa
lolos sementara unduhannya gagal." Kalimat itu ditulis sebagai KLAIM yang belum
terbukti. Sesi 25 membuktikannya: tiga pasar bernama huruf Tionghoa, 19
berkas-bulan. Jadi peringatannya benar, dan kodenya sudah mematuhi peringatan
itu sejak awal.

Ini membalik dugaan saya tentang letak bahayanya. Yang lalai bukan `arsip.py`,
melainkan `ringkas_semesta.py` — alat ukur yang saya tulis BELAKANGAN, yang
memasang `[A-Z0-9_]` tanpa alasan dan membuang tiga entitas sah tanpa jejak.
Modul lama lebih berhati-hati daripada modul baru. Pelajarannya: kehati-hatian
tidak diwariskan antar modul hanya karena berada di repo yang sama.

## Utang 7 — status baru

- Titik (a) URL arsip: **DIBAYAR untuk `arsip.py`**, terbukti dari pembacaan
  baris, bukan dari ingatan.
- Titik (b) nama berkas keluaran: **BELUM**, dan bukan milik `arsip.py`. Ia ada
  di pemanggil dan di `klines.tulis_parquet`. Aturan 20 melarang saya
  menyimpulkan lapisan itu aman hanya karena `arsip.py` aman.
- Titik (c) kunci manifes: **BELUM**, dan manifesnya memang belum ditulis
  (bagian dari utang 24). Harus lahir sudah sadar non-ASCII, bukan ditambal.

Uji dengan kasus positif dan negatif memakai ketiga nama Tionghoa itu (aturan
12) BELUM ada; `segmen` belum punya uji. Itu tetap terbuka.

## Berikutnya

1. Uji `segmen`, `url_klines`, `url_funding` dengan ketiga nama sungguhan
   (kasus positif) dan nama ASCII biasa (kasus negatif).
2. Periksa `klines.py` untuk titik (b).
3. Utang 24 (serapan penuh) dengan manifes yang sadar non-ASCII sejak lahir.

Ramalan berikutnya **R-59**.
