# Jurnal 2026-07-28 sesi 30

## Pekerjaan sesi ini

Utang 25 / H-A003: siapa yang menulis ulang `reports/semesta_bulan_1m.json`
secara berkala? Empat kali dibaca, byte SELALU 18.884, tetapi `sidik_data`
berubah setiap kali: `fae1210f…` (08:56) → `f435f470…` (09:09) → `7d287ab6…`
(09:23) → `ced89c14…` (09:34). H-A003 menduga hanya `waktu_utc` yang berubah dan
penulisnya adalah workflow survei yang berjalan berulang.

## Pra-registrasi (DITULIS SEBELUM BERKAS DIBACA)

Didorong lebih dulu pada commit `3bf811f665fb00fe7b5158ae46116243f10d33cc`.

- **R-65.** `survei_semesta.yml` memuat pemicu terjadwal (`schedule:` +
  `cron:`). Besaran pendamping: cacah baris `cron:` adalah 1 sampai 2.
- **R-66.** Workflow itu menulis `reports/semesta_bulan_1m.json` dan
  meng-commit-nya dengan pesan bertanda `[skip ci]`.
- **R-67.** Workflow yang sama juga menulis `reports/semesta_rentang.json`.

## Yang dibaca

`.github/workflows/survei_semesta.yml`, blob
`b6a898b74c199736d66a22613b292ca201dd68a3`, dibaca UTUH. Penyebut (aturan 30):
seluruh berkas.

## Adjudikasi

- **R-65 MELESET.** TIDAK ADA `schedule:` dan tidak ada satu pun baris `cron:`.
  Pemicunya hanya `push` pada `lux_ai/serapan/survei.py` dan pada berkas
  workflow itu sendiri, ditambah `workflow_dispatch`. Cacah `cron:` = **0**, di
  luar pita 1..2.
- **R-66 MELESET SEPARUH.** Bagian `[skip ci]` TEPAT: pesan commitnya
  `"laporan survei semesta ${GITHUB_RUN_ID} [skip ci]"`. Bagian "workflow itu
  menulis `semesta_bulan_1m.json`" TIDAK TERBUKTI dari berkas ini: yml hanya
  memanggil `python -m lux_ai.serapan.survei` lalu `git add reports` — seluruh
  direktori, tanpa menyebut nama berkas mana pun. Penulis sesungguhnya ada di
  `survei.py`, yang belum saya baca.
- **R-67 TIDAK TERADJUDIKASI.** Cacat rancangan ramalan, sekeluarga dengan R-40:
  saya mematok cakupan pada yml (aturan 20), padahal yml tidak pernah menyebut
  nama berkas laporan. Pertanyaannya tidak bisa dijawab oleh penyebut yang saya
  pilih sendiri. Ini kesalahan saya, bukan kekurangan datanya.

Papan skor R-1..R-67: TEPAT 37, MELESET **19**, MELESET SEPARUH **3**, TIDAK
TERADJUDIKASI **2**, MENUNGGU 6. 37+19+3+2+6 = 67 (aturan 21).

## H-A003 — status

**GUGUR dalam bentuk yang saya tulis.** Tidak ada penjadwalan; berkas itu tidak
ditulis ulang "secara berkala" oleh workflow ini. Yang tersisa adalah teka-teki
yang lebih tajam, bukan jawaban:

Keempat perubahan `sidik_data` terjadi 08:56–09:34, tepat ketika saya mendorong
`ringkas_semesta.py` dan `bentuk_semesta.py` — BUKAN `survei.py`. Jadi
`survei_semesta.yml` mustahil terpicu oleh dorongan itu. Namun setiap workflow
di repo ini menutup dengan `git add reports`, yang mementaskan SELURUH
direktori. Dugaan baru: sesuatu di dalam run workflow lain menulis ulang berkas
itu di runner, lalu `git add reports` ikut membawanya serta tanpa disengaja.
**Ini memerlukan verifikasi**; ia dugaan, bukan temuan.

Tersangka yang harus dibaca sesi berikutnya, berurutan: `lux_ai/serapan/
survei.py` (apakah ia menulis saat diimpor, bukan saat dijalankan), lalu
`.github/workflows/ringkas_semesta.yml` dan `bentuk_semesta.yml`.

Catatan yang lebih penting daripada teka-tekinya: `git add reports` adalah
kebiasaan yang membuat setiap workflow bisa meng-commit perubahan yang bukan
miliknya. Itu cacat kebersihan yang nyata, dan ia menjelaskan mengapa asal-usul
sebuah angka bisa kabur. Jalur serapan (utang 24) TIDAK boleh memakai pola itu;
ia harus menambahkan berkas yang disebut namanya satu per satu.

## Berikutnya

1. Baca `survei.py`, lalu dua workflow tersangka. Ramalan berikutnya **R-68**.
2. STATE v17 (baca v16 utuh lebih dulu, jangan dari ingatan).
3. Utang 24 dengan empat syarat rancangan PROMPT v17 — ditambah syarat kelima:
   commit hanya berkas yang disebut namanya, bukan `git add reports`.
