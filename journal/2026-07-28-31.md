# Jurnal 2026-07-28 sesi 31

## Pekerjaan sesi ini

Melanjutkan utang 25 setelah H-A003 gugur (jurnal 30). Pertanyaannya kini bukan
"apakah terjadwal" melainkan "siapa yang menulis ulang
`reports/semesta_bulan_1m.json` di dalam run workflow yang bukan miliknya".

## Pra-registrasi (DITULIS SEBELUM KETIGANYA DIBACA)

Didorong lebih dulu pada commit `9d6704844229a303ebe4e6074f023a3252cae3e0`.

- **R-68.** `survei.py` menulis laporannya HANYA di dalam fungsi, bukan pada
  tingkat modul saat diimpor. Besaran pendamping: cacah penulisan berkas pada
  tingkat modul adalah tepat **0**.
- **R-69.** Kedua workflow tersangka menutup dengan `git add reports`. Besaran
  pendamping: **2 dari 2**.
- **R-70.** Tidak satu pun dari kedua workflow itu memanggil atau mengimpor
  `survei`. Besaran pendamping: cacah kemunculan kata `survei` adalah **0**.

## Yang dibaca

Tiga berkas, seluruhnya UTUH (aturan 30 — penyebut: isi penuh ketiganya):

- `lux_ai/serapan/survei.py`, blob `26b14940761d0469875d873cc19666274e936edf`
- `.github/workflows/ringkas_semesta.yml`, blob `d6145d289459914d64b6f1c11a4af075c00f7f18`
- `.github/workflows/bentuk_semesta.yml`, blob `dc393dd0a27d00d3ae45dff077716e7f0c7c2d84`

## Adjudikasi

- **R-68 TEPAT.** Seluruh penulisan di `survei.py` lewat `_tulis`, dan `_tulis`
  hanya dipanggil dari dalam `_progres`, `survei_rentang`, dan `jalankan`.
  Tingkat modul hanya memuat tetapan. Cacah = **0**.
- **R-69 MELESET.** KEDUANYA justru menyebut nama berkas satu per satu:
  `git add reports/ringkas_semesta.json reports/ringkas_semesta.log
  reports/ringkas_semesta_status.json`, dan tiga berkas analog untuk bentuk.
  Cacah `git add reports` polos = **0 dari 2**, bukan 2 dari 2.
- **R-70 TEPAT.** Kata `survei` tidak muncul sama sekali di kedua yml. Cacah
  = **0**.

Papan skor R-1..R-70: TEPAT **39**, MELESET **20**, MELESET SEPARUH 3, TIDAK
TERADJUDIKASI 2, MENUNGGU 6. 39+20+3+2+6 = 70 (aturan 21).

## KOREKSI atas jurnal 30 — saya menulis klaim yang salah

Jurnal 30 berbunyi: "setiap workflow di repo ini menutup dengan `git add
reports`". **Itu SALAH.** Saya menyamaratakan dari dua berkas yang kebetulan
saya baca (`penyebut_kc6.yml` dan `survei_semesta.yml`) ke seluruh repo, persis
jenis pelanggaran yang aturan 20 larang — dan saya melakukannya di jurnal yang
pada paragraf lain justru mengutip aturan 20.

Fakta yang benar, dari empat workflow yang kini terbaca: dua memakai `git add
reports` polos (`penyebut_kc6`, `survei_semesta`), dua menyebut nama berkas satu
per satu (`ringkas_semesta`, `bentuk_semesta`). Dua dari empat, bukan empat dari
empat. Sisanya (`ci`, `probe_serapan`, `diagnosa_kc6`, `rentang_kc6`,
`uji_resample`) BELUM dibaca dan tidak boleh saya hitung ke pihak mana pun.

Syarat rancangan kelima di jurnal 30 tetap berlaku, tetapi alasannya berubah:
bukan "perbaiki kebiasaan buruk yang menyeluruh", melainkan "pakai pola baik
yang SUDAH ADA di separuh workflow".

## Temuan yang lebih penting: `survei.py` bukan penulisnya

`survei.py` hanya menulis tiga berkas: `reports/survei_semesta.json`,
`reports/semesta_rentang.json`, `reports/survei_progres.json`. Ia TIDAK PERNAH
menulis `reports/semesta_bulan_1m.json`. Ditambah R-69 dan R-70, ketiga
tersangka sesi ini bersih:

- `survei.py` tidak menulis berkas itu.
- Kedua workflow tersangka tidak bisa meng-commitnya, sebab keduanya hanya
  meng-add berkas bernama miliknya sendiri.

## H-A004 (menggantikan H-A003)

Penulisnya berjalan di dalam **CI**, bukan di workflow laporan. Dasarnya: waktu
perubahan `sidik_data` (08:56, 09:09, 09:23, 09:34) berimpit dengan run CI, dan
CI menjalankan pytest atas seluruh `tests/`. Bila ada uji atau modul yang
menulis ke `reports/` saat pytest berjalan, dan `ci.yml` meng-commit direktori
itu polos, seluruh gejalanya terjelaskan: byte tetap 18.884 karena hanya
`waktu_utc` yang berubah.

Ini HIPOTESIS, bukan temuan. Ia bisa gugur.

## Pra-registrasi berikutnya (SEBELUM `ci.yml` DIBACA)

- **R-71.** `.github/workflows/ci.yml` meng-commit dengan `git add reports`
  polos, bukan menyebut nama berkas. Besaran pendamping: cacah `git add reports`
  polos di berkas itu adalah **1**.
- **R-72.** `ci.yml` menjalankan pytest atas seluruh direktori `tests/` tanpa
  penyaring, sehingga uji apa pun yang menulis ke `reports/` akan ikut terbawa.
- **R-73.** Besaran pendamping H-A004, tidak bersyarat (aturan 27): cacah berkas
  di bawah `reports/` yang disebut namanya secara eksplisit di `ci.yml` adalah 0
  sampai 2.

Medan penggugur (aturan 24): bila `ci.yml` ternyata menyebut nama berkasnya satu
per satu, H-A004 GUGUR dan penulisnya harus dicari di antara empat workflow yang
belum dibaca — bukan diasumsikan.

## Adjudikasi R-71..R-73

Belum. Diisi setelah `ci.yml` dibaca.
