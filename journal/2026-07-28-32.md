# Jurnal 2026-07-28 sesi 32

## Yang dibaca

`.github/workflows/ci.yml`, blob `c79497b2c812679eaa69aee5b3160eac9f5c5fb7`,
UTUH. Penyebut (aturan 30): seluruh berkas.

## Adjudikasi R-71..R-73

- **R-71 MELESET.** `ci.yml` TIDAK memakai `git add reports` polos. Ia menyebut
  dua berkas: `git add reports/ci_terakhir.json reports/ci_terakhir.txt`. Cacah
  `git add reports` polos = **0**, diramalkan 1.
- **R-72 TEPAT.** `python -m pytest -q` dijalankan tanpa penyaring direktori,
  jadi seluruh `tests/` ikut. Ada pula langkah `--collect-only` yang dicetak
  SEBELUM pytest, sesuai kebiasaan cacah uji.
- **R-73 TEPAT.** Berkas di bawah `reports/` yang disebut namanya di `ci.yml`
  ada **2** (`ci_terakhir.json`, `ci_terakhir.txt`), di dalam pita 0..2.

Papan skor R-1..R-73: TEPAT **41**, MELESET **21**, MELESET SEPARUH 3, TIDAK
TERADJUDIKASI 2, MENUNGGU 6. 41+21+3+2+6 = 73 (aturan 21).

## H-A004 GUGUR

Hipotesis "CI yang meng-commit `semesta_bulan_1m.json` lewat `git add reports`
polos" TIDAK BISA benar: CI hanya meng-add dua berkas bernama, dan `ci.yml`
bahkan memasang `paths-ignore: reports/**` sehingga perubahan di `reports/`
tidak memicunya. R-73 TEPAT tetapi mekanisme intinya salah — besaran yang jatuh
di dalam pita tidak menyelamatkan hipotesis yang keliru.

Dua hipotesis berturut-turut gugur (H-A003, H-A004). Keduanya saya susun dari
penalaran "pola repo ini biasanya begini", dan dua kali pola itu ternyata tidak
seragam. Itu sendiri temuan: **repo ini TIDAK punya satu pola workflow; ia punya
setidaknya dua mazhab** — `git add reports` polos (`penyebut_kc6`,
`survei_semesta`) lawan berkas bernama (`ringkas_semesta`, `bentuk_semesta`,
`ci`). Tiga banding dua dari lima yang terbaca; empat workflow belum dibaca.

## Berhenti menebak, mulai membaca riwayat

Cara termurah menjawab "siapa menulis ulang berkas itu" bukan membaca sisa
workflow satu per satu, melainkan membaca RIWAYAT COMMIT berkas itu sendiri.
Riwayat menyebut penulis, waktu, dan pesan commit sekaligus.

## Pra-registrasi (SEBELUM RIWAYAT DIBACA)

- **R-74.** Riwayat commit `reports/semesta_bulan_1m.json` memuat sedikitnya satu
  commit pada 2026-07-28 antara 08:50Z dan 09:40Z. Besaran pendamping
  (aturan 26): cacah commit yang menyentuh berkas itu dalam rentang tersebut
  adalah **3 sampai 6** (saya mencatat empat `sidik_data` berbeda, jadi minimal
  tiga peralihan).
- **R-75.** Pengarang commit-commit itu adalah akun bot CI (`lux-ci` atau
  `github-actions[bot]`), bukan saya lewat `push_files`.
- **R-76.** Pesan commitnya menyebut workflow bermazhab `git add reports` polos,
  yakni survei atau penyebut KC-6.

Medan penggugur (aturan 24): bila cacah commit dalam rentang itu **0**, maka
berkas itu TIDAK PERNAH berubah, dan keempat `sidik_data` yang saya catat di
jurnal-jurnal sebelumnya adalah kesalahan pencatatan SAYA — bukan gejala sistem.
Itu kemungkinan yang paling memalukan, dan justru karena itu ia harus ditulis
sebagai hasil yang sah, bukan disingkirkan.

Penyebut (aturan 30): seluruh commit yang dikembalikan penyaring path pada
rentang waktu itu.

## Adjudikasi R-74..R-76

Belum. Diisi setelah riwayat dibaca.
