# Jurnal 63 — 2026-07-29

## Koreksi R-148, didaftarkan SEBELUM laporannya mendarat

Jurnal 62 meramalkan CI melaporkan **198** uji: 190 + "8 uji
`tests/test_pulihkan.py`". Angka itu salah, dan saya menemukannya saat membaca
ulang ekor berkas yang baru didorong — bukan setelah melihat hasilnya.

Berkas itu memuat **8 fungsi uji**, tetapi salah satunya berparameter:

```
@pytest.mark.parametrize("nama, aman", [ ...empat kasus... ])
def test_anggota_aman_menolak_jalur_berbahaya(nama, aman)
```

pytest mengumpulkan BUTIR, bukan fungsi. Tujuh fungsi biasa + empat butir
berparameter = **11 butir**. Jadi cacah yang benar adalah 190 + 11 = **201**.

Saya menghitung fungsi lalu menuliskannya sebagai uji — persis jenis kekeliruan
yang melahirkan aturan 38 (cacah uji hanya sah dari `reports/ci_terakhir.json`,
bukan dari penjumlahan taksiran saya). Aturan itu dibuat karena hal yang sama
pada angka 135 lawan 141, dan saya mengulanginya lagi di sini.

**Status R-148: MELESET.** Ia diramalkan 198; angka yang saya harapkan sekarang
201. Aturan 29 melarang menghapus: ramalan 198 tetap berdiri di jurnal 62 apa
adanya, dan koreksi ini berdiri di sampingnya. Bila CI ternyata melaporkan 198,
koreksi inilah yang salah dan itu pun akan dicatat.

Ramalan pengganti, didaftarkan sekarang:

- **R-149.** `reports/ci_terakhir.json` untuk commit `ab4e0774` melaporkan
  `cacah_uji` **201** dan `kode_keluar` **0**.

Catatan kejujuran: R-149 lebih berisiko daripada R-148 tampak, karena delapan
uji itu belum pernah dijalankan sekali pun — sandbox tidak punya jaringan dan
tidak menjalankan pytest. Uji yang memakai `shutil.move` atas glob `data/rilis`
dan pembongkaran tar nyata bisa saja gagal pada hal remeh. Bila ada yang gagal,
`kode_keluar` 1 dan R-149 MELESET pada kedua medannya.

## Yang sedang berjalan

Commit `ab4e0774` menyalakan `pulihkan-rilis` (matriks 0..7) dan CI. Belum ada
laporan yang bisa dibaca. Yang menentukan nanti: `reports/pulihkan_pecahan_<i>.json`
dan `_status.json`, dicocokkan lewat `run_id` dan `sidik_manifes` — bukan lewat
keberadaan berkas.
